import 'dart:ui';

class Reminder {
  final String id;
  final String title;
  final String description;
  final DateTime date;
  final Color color;

  Reminder(this.id, this.title, this.description, this.date, this.color);
}
